fastapi==0.116.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.43
pymysql==1.1.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic-settings==2.10.1
python-dotenv==1.1.1
email-validator==2.3.0
slowapi==0.1.9

# ML engine (PR2).
# torch CPU wheels are hosted on a separate index. Install torch first with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1
# then run `pip install -r requirements.txt` for the rest. GPU users can skip
# the extra index and just install torch from PyPI.
torch==2.5.1
torch-geometric==2.6.1
scikit-learn==1.5.2
numpy==1.26.4
